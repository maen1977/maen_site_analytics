name: HTML aware fix Spider prices

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "scripts/html-aware-fix-spider-prices.mjs"
      - ".github/workflows/html-aware-fix-spider-prices.yml"

permissions:
  contents: write

concurrency:
  group: html-aware-fix-spider-prices
  cancel-in-progress: true

jobs:
  html-aware-fix-spider-prices:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Check script
        run: node --check scripts/html-aware-fix-spider-prices.mjs

      - name: Fix Spider prices in HTML
        run: node scripts/html-aware-fix-spider-prices.mjs

      - name: Commit fixed prices
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add public/index.html public/index_phone.html scripts/html-aware-fix-spider-prices.mjs .github/workflows/html-aware-fix-spider-prices.yml

          if [ -n "$(git status --porcelain)" ]; then
            git commit -m "Fix Spider T777 and T666 prices in HTML"
            git push
          else
            echo "No changes to commit."
          fi
