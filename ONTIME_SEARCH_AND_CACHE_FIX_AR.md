# إصلاح ظهور ترددات ON Time / ON Sport والبحث بعد تغيير الاسم أو التردد

تم تنفيذ هذا الإصلاح بعد ملاحظة أن بعض القنوات، مثل قنوات ON Time Sports / ON Sport، قد لا تظهر بالاسم الجديد أو قد يظهر تردد قديم قبل التردد الحالي.

## سبب المشكلة

1. محرك البحث كان صارمًا جدًا ويبحث داخل اسم القناة نفسه أكثر من اعتماده على المرادفات.
   لذلك قناة باسم `ON Sport HD` لم تكن تُطابق دائمًا بحث المستخدم عن `اون تايم سبورت`.

2. باقة ON Time / ON Sport لم تكن موجودة كفلتر باقة صريح داخل `PACKAGE_KEYWORDS`، فكان البحث يسقط أحيانًا إلى بحث نصي حر.

3. الموقع يستخدم ملفات بيانات بإصدار ثابت مثل:
   `frequency-data.v20260528.json`
   وهذا ممتاز لتوفير الكريدت والكاش، لكن عند تعديل `frequency-data.json` فقط بدون توليد إصدار جديد، قد يبقى الزائر يشاهد الملف القديم.

## ما تم تعديله

- إضافة باقة `ontime` إلى فلاتر الباقات الذكية.
- إضافة مرادفات عربية وإنجليزية:
  - اون تايم
  - أون تايم
  - اون تايم سبورت
  - أون سبورت
  - ON Time Sports
  - ON Sport
  - ON Sports
- رفع أولوية الترددات الحالية لقنوات ON/ON Time.
- إبقاء الترددات القديمة/الاحتياطية لكن بترتيب أقل حتى لا تظهر قبل الترددات الحالية.
- إخفاء تردد Legacy قديم من بحث ON العادي، مع بقائه متاحًا عند البحث برقم التردد نفسه.
- تحديث بيانات ON Time / ON Sport الحالية:
  - نايل سات 11977 V 27500 لقنوات ON Sport HD / Plus / Max.
  - نايل سات 11823 V 27500 كقائمة حالية/مساندة لـ ON Time Sports HD.
  - عربسات/بدر 12379 H 27500 لقناة ON Time Sports HD Arabsat.
- توليد ملف إصدار جديد:
  `frequency-data.v20260530.json`
- تحديث `frequency-manifest.json` حتى لا يبقى Cloudflare أو المتصفح على نسخة قديمة.
- تحديث اختبارات البحث للتأكد من أن بحث `اون تايم سبورت` يرجع التردد الحالي 11977.

## مهم عند أي تعديل مستقبلي على الترددات

بعد تعديل `public/frequencies/frequency-data.json` يدويًا، لا يكفي رفع الملف وحده. يجب تشغيل:

```bash
npm run github:frequency-assets
```

أو:

```bash
npm run build:static
```

هذا يولّد ملف versioned جديد ويحدّث `frequency-manifest.json`. بدون هذه الخطوة قد يستمر الموقع بعرض نسخة قديمة بسبب الكاش.

## فحص الإصلاح

تم فحص المشروع عبر:

```bash
npm run build:static
npm run check
node scripts/search-smoke-tests.mjs
```

وتتضمن الاختبارات الآن تأكيدًا أن:

- `اون تايم سبورت` يرجع تردد نايل سات الحالي 11977 V.
- `on sport` يرجع قنوات Plus / Max على 11977.
- تردد عربسات 12379 موجود.
