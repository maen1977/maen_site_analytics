# إضافة قسم كأس العالم 2026

تمت إضافة قسم جديد باسم **كأس العالم 2026** داخل الموقع للكمبيوتر والموبايل.

## ما الذي تمت إضافته؟

- هيدر عصري خاص بالأردن مع علم أردني بتأثير رفرفة خفيف عبر CSS.
- عبارة: **الأردن في قلب الحدث**.
- عرض مباريات اليوم، مباريات الأردن، كل المباريات، المجموعات، أفضل الثوالث، والأدوار الإقصائية.
- بحث عربي/إنجليزي داخل البطولة.
- ملفات بيانات مستقلة داخل `public/worldcup-2026/`.
- GitHub Action لتحديث النتائج والبيانات كل 12 ساعة.

## التحديث التلقائي

الملف:

`.github/workflows/update-worldcup-2026.yml`

يعمل مرتين يومياً بتوقيت الأردن مباشرة:

- `03:00` فجراً
- `15:00` عصراً

وذلك باستخدام `timezone: "Asia/Amman"` داخل GitHub Actions.

ويمكن تشغيله يدوياً من GitHub عبر زر **Run workflow**.

## مصدر البيانات

السكريبت يستخدم مصدر OpenFootball المفتوح كطبقة بيانات عملية، مع حفظ رابط الجدول الرسمي من FIFA كمرجع داخل metadata.

الملف المسؤول:

`scripts/update-worldcup-2026.mjs`

## ملاحظة مهمة

قبل بداية البطولة تظهر النتائج 0 - 0 مع حالة "لم تبدأ". عندما يحدّث المصدر النتائج بعد المباريات، يقوم GitHub Action بتحديث ملفات JSON ويعمل commit تلقائي إذا حصل تغيير.
