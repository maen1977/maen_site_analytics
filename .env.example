# Shared analytics settings
ANALYTICS_SALT=replace-with-long-random-secret
ANALYTICS_TIMEZONE=Asia/Amman

# Resend email settings used by GitHub Actions
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxx
REPORT_EMAIL=your-report-email@example.com
REPORT_FROM=Maen Analytics <onboarding@resend.dev>
FREQUENCY_REPORT_EMAIL=your-frequency-report-email@example.com

# Cloudflare D1 settings used by GitHub Actions for the daily visitor report
CLOUDFLARE_ACCOUNT_ID=your-cloudflare-account-id
CLOUDFLARE_D1_DATABASE_ID=your-d1-database-id
CLOUDFLARE_API_TOKEN=your-cloudflare-api-token-with-d1-read-access

# Cloudflare Pages binding name must be MAEN_DB for visitor tracking.
# Optional: restrict browser tracking requests to your domain.
# ALLOWED_ORIGIN=https://maensat.pages.dev

# Optional: enable Turnstile verification for visitor search feedback
TURNSTILE_SECRET_KEY=0x0000000000000000000000000000000AA
# Frontend site key is public; set window.MAEN_TURNSTILE_SITE_KEY in the page if you enable it.

# Optional frequency cleanup rules
# 1 = remove frequencies/channels when trusted sources agree they are closed and no current source confirms them.
FREQUENCY_REMOVE_CLOSED_CONSENSUS=1
# Minimum independent sources required before automatic closed-frequency deletion. Official/operator source can act alone.
FREQUENCY_CLOSED_MIN_SOURCES=2
FREQUENCY_CLOSED_MIN_SOURCES_OFFICIAL=1

# Safer daily frequency cleanup rules
# How many successful daily scans a missing transponder must miss before automatic deletion.
FREQUENCY_REMOVE_MISSING_AFTER_CHECKS=3
# If too many source rows are incomplete (system/mod missing), deletion is skipped for safety.
FREQUENCY_MAX_INCOMPLETE_SYSTEM_MOD_FOR_REMOVAL=25
FREQUENCY_MAX_INCOMPLETE_SYSTEM_MOD_RATIO_FOR_REMOVAL=0.10

# Optional World Cup broadcaster source.
# You can either set a remote URL in GitHub Variables, or edit public/worldcup-2026/broadcast-source.json locally.
# WORLD_CUP_2026_BROADCAST_SOURCE_URL=https://raw.githubusercontent.com/maen1977/maen_site_analytics/main/public/worldcup-2026/broadcast-source.json
