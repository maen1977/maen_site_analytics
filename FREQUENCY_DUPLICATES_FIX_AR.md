# إصلاح تكرار المحطات في نتائج بحث الترددات

## السبب
كان قسم الترددات يعرض بطاقة منفصلة لكل نتيجة من نوع: محطة + تردد.
هذا جعل نفس المحطة تظهر أكثر من مرة عندما تكون موجودة على أكثر من تردد أو أكثر من قمر، مثل بعض قنوات MBC أو CNN أو القنوات السعودية.

هذا ليس تكراراً خاطئاً دائماً في قاعدة البيانات؛ أحياناً هو نفس اسم القناة وله أكثر من تردد صحيح.
لكن تجربة المستخدم كانت تبدو وكأن النتائج مكررة.

## التعديل
تم تعديل عرض نتائج البحث ليجمع النتائج حسب اسم المحطة:

- تظهر كل محطة مرة واحدة فقط.
- داخل بطاقة المحطة يظهر عدد الترددات المتاحة لها.
- تظهر أهم الترددات داخل نفس البطاقة.
- لا يتم حذف أي تردد من قاعدة البيانات.
- لا يوجد أي API جديد ولا أي استهلاك إضافي على Netlify أو Cloudflare.

## الملفات المعدلة
- public/index.html
- public/index_phone.html

## الفحص
تم فحص JavaScript المضمّن داخل HTML، وتم تشغيل:

```bash
npm run check
node scripts/search-smoke-tests.mjs
```
