# تحديث البحث الذكي في قسم الترددات

تم تنفيذ طبقة بحث وتصنيف ذكية لقسم الترددات مع الحفاظ على عرض النتائج داخل جدول الترددات نفسه، بدون تحويلها إلى بطاقات عند استخدام FTA أو البحث النصي.

## ما تم تعديله

- جعل القمر الافتراضي عند فتح القسم: **Nilesat / نيل سات**.
- جعل فلتر الخدمة الافتراضي: **FTA / قنوات مجانية**.
- البحث أصبح يلتزم بالقمر المختار أولاً، ثم فلتر FTA/مشفر، ثم الدولة أو التصنيف أو اسم القناة.
- عند كتابة MBC مع اختيار Nilesat لا تظهر نتائج MBC من Arabsat/BADR أو أي قمر آخر.
- عند البحث عن دولة مثل: مصرية، إيطالية، لبنانية… يتم البحث داخل القمر المختار فقط.
- عند البحث عن قنوات مسيحية أو إسلامية يتم استخدام تصنيف صارم حتى لا تختلط القنوات المسيحية والإسلامية.
- تم إصلاح مشكلة الصفوف المختلطة: لم يعد بلد القناة يؤخذ من كلمات الصف/التردد كامل، بل من بيانات القناة نفسها أو قواعد اسم القناة فقط.
- تم إنشاء ملف تصنيف مستقل: `public/frequencies/channel-classification.json`.

## أرقام الداتا بعد التحديث

- عدد صفوف الترددات: 363
- عدد مراجع المحطات داخل الصفوف: 2677
- عدد أسماء المحطات الفريدة: 2211
- محطات فريدة لها بلد/منطقة مصنفة: 867
- كل المحطات لديها تصنيف نوعي عام أو متخصص: 2211

## أمثلة تم فحصها

- Nilesat + FTA + `MBC`: يعرض MBC الموجودة على نيل سات فقط.
- Arabsat/BADR + FTA + `MBC`: يعرض MBC الموجودة على عربسات/بدر فقط.
- Nilesat + FTA + `مصرية`: يعرض القنوات المصرية على نيل سات فقط، ولا يسحب CGTN أو تلفزيون لبنان من نفس التردد المختلط.
- Nilesat + FTA + `إيطالية`: لا يعرض قنوات إيطالية إذا لم توجد على نيل سات.
- Nilesat + FTA + `مسيحية`: يعرض القنوات المسيحية فقط.
- Nilesat + FTA + `إسلامية`: يعرض القنوات الإسلامية فقط.

## ملاحظة مهمة

التصنيف مبني على الداتا الحالية + قواعد أسماء عالية الثقة + مصادر ترددات وقوائم قنوات عامة مثل LyngSat وKingOfSat وNilesat. المحطات غير الواضحة لا تُدخل في التصنيفات الحساسة مثل مسيحية/إسلامية إلا إذا كان التصنيف واضحاً.
