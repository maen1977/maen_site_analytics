# ترقية احترافية صديقة للكريدت — 2026-05-30

تم تعديل الموقع ليبقى مناسبًا لخطة Netlify وCloudflare المجانية قدر الإمكان. الفكرة الأساسية: البحث والاقتراحات تعمل داخل المتصفح من ملفات JSON ثابتة، والـ APIs لا تُستخدم إلا للأشياء قليلة العدد مثل كلمات البحث التي لم تجد نتائج.

## ما تم تنفيذه

1. **تفعيل اقتراحات البحث الذكي**
   - تم إلغاء كود الإخفاء القديم لقائمة الاقتراحات.
   - الاقتراحات تظهر للقنوات، الترددات، الباقات، الدول، والتصنيفات.
   - تعمل من البيانات المحمّلة داخل المتصفح بدون Netlify Functions.

2. **تخفيف حجم الصفحة**
   - تم حذف قاعدة الترددات المدمجة من `public/index.html` و `public/index_phone.html`.
   - أصبحت الصفحة تحمل البيانات من ملفات ثابتة داخل `public/frequencies/`.
   - تم إنشاء:
     - `frequency-manifest.json`
     - `frequency-data.vYYYYMMDD.json`
     - `search-index.json`
   - هذا يقلل حجم HTML ويخلي الكاش أقوى.

3. **تحميل بيانات versioned لتقليل السحب**
   - الصفحة تقرأ manifest صغير أولًا، ثم ملف JSON بإصدار تاريخي.
   - الملف versioned يصلح للكاش الطويل، بدل إعادة تحميل نفس الملف الكبير كثيرًا.

4. **بحث طبيعي أذكى**
   - البحث صار يفهم عبارات مثل:
     - `قنوات رياضة مجانية نايل سات`
     - `bein hotbird`
     - `قنوات اسلامية نايل سات`
   - إذا ذكر المستخدم قمرًا داخل البحث، يتم استخدامه كفلتر حتى لو قائمة القمر على “كل الأقمار”.

5. **تسجيل البحث الذي لم يجد نتائج**
   - لا يتم تسجيل كل بحث ولا كل حرف.
   - يسجل فقط عندما يكون البحث 3 أحرف أو أكثر ولا توجد نتائج.
   - يوجد debounce وتأخير وتكرار يومي محدود عبر `localStorage`.
   - التخزين في Cloudflare D1 يكون مجمّعًا في جدول `failed_searches_daily` بدل تخزين كل ضغطة.

7. **شارات ثقة للترددات**
   - تظهر شارات مثل: `ثقة عالية`، `آخر فحص`، `مصدر محفوظ` حسب بيانات المصدر الموجودة داخل كل تردد.

9. **اختبارات بحث ثابتة**
   - أضيف `scripts/search-smoke-tests.mjs`.
   - يفحص أمثلة مهمة مثل:
     - الجزيرة
     - beIN Sports
     - 11766
     - عدم خلط `rai` مع Bahrain بسبب substring
     - وجود نتائج لـ “رياضة مجانية على نايل سات”

10. **تحكم أفضل في Analytics**
   - تتبع pageview المخصص إلى D1 أصبح مطفأ افتراضيًا لحماية الكريدت.
   - يمكن تشغيله يدويًا بوضع:
     ```html
     <script>window.MAEN_ANALYTICS_MODE = "session";</script>
     ```
     قبل سكربت analytics، ليعمل مرة واحدة لكل صفحة في الجلسة.

## إعداد Cloudflare المطلوب

في Cloudflare Pages أضف المتغيرات/الأسرار التالية عند الحاجة:

```env
MAEN_DB = D1 binding
ANALYTICS_SALT = long-random-secret
ADMIN_ALLOWED_ORIGIN = https://maensat.pages.dev
TURNSTILE_SECRET_KEY = optional-turnstile-secret
```

ثم شغّل SQL من:

```text
cloudflare/schema.sql
```

## أوامر مهمة

```bash
npm run github:frequency-assets
npm run check
```

## ملاحظات كريدت مهمة

- البحث والاقتراحات لا تستخدم API.
- تسجيل البحث الفاشل يحدث فقط عند 0 نتائج، وليس مع كل كتابة.
- ملفات الترددات أصبحت قابلة للكاش بشكل أفضل.
- Netlify بقي static hosting فقط؛ كل endpoints الحساسة يتم تحويلها إلى Cloudflare Pages.
