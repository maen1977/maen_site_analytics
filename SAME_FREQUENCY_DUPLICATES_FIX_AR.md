# إصلاح تكرار نفس المحطة على نفس التردد

## سبب المشكلة
كان البحث السابق يزيل تكرار المحطة عندما تكون موجودة على أكثر من تردد، لكنه لم يكن يحمي بشكل كافٍ من تكرار نفس اسم القناة داخل نفس صف التردد.

ظهور نفس الاسم على نفس القمر ونفس التردد يمكن أن يحدث بسبب:

1. دمج بيانات الترددات من أكثر من مصدر مثل LyngSat / DTHSat / KingOfSat.
2. وجود نفس اسم القناة داخل مصفوفة `channels` أو داخل النص المجمع `channel`.
3. عدم وجود مفتاح فريد صارم يجمع الصفوف حسب: القمر الفعلي + المدار + التردد + الاستقطاب + SR + FEC + النظام.
4. دالة تقسيم أسماء القنوات كانت تعرض الأسماء كما وصلت بدون إزالة تكرار داخلي.

## ماذا تم تعديله
تم تعديل `public/index.html` و `public/index_phone.html` كالتالي:

- إضافة `uniqueChannelNames()` لإزالة تكرار أسماء القنوات داخل نفس التردد.
- إضافة `channelDisplayDedupeKey()` لمقارنة الأسماء بشكل آمن مع الحفاظ على الأسماء المختلفة فعليًا مثل `Svoboda` و `Svoboda+`.
- إضافة `frequencyRowDedupeKey()` لدمج أي صفوف متطابقة فعليًا بنفس القمر والتردد والاستقطاب والـ SR.
- تعديل عرض جدول الترددات حتى لا يعرض نفس الشريحة مرتين.
- تعديل بطاقات نتائج المحطات حتى تجمع الترددات بدون تكرار داخلي.
- تعديل التقسيم حتى لا يعتبر علامة `+` داخل اسم القناة فاصلاً إلا إذا كانت بين مسافات، حتى لا تتكسر أسماء مثل Canal+ أو Svoboda+.

## النتيجة
إذا تكرر نفس اسم القناة على نفس القمر ونفس التردد ونفس الاستقطاب، سيظهر مرة واحدة فقط.
أما إذا كانت القناة موجودة فعليًا على تردد آخر أو استقطاب آخر، تبقى ظاهرة كتردد إضافي داخل بطاقة واحدة.

## الفحص
تم تشغيل:

```bash
npm run check
node scripts/search-smoke-tests.mjs
```

وكلاهما نجح بدون أخطاء.
