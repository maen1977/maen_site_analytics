# إصلاح تاريخ قسم كأس العالم 2026

تم تعديل قسم كأس العالم بحيث لا يخلط بين:

- **آخر فحص**: آخر مرة شغّل فيها GitHub Actions السكربت وراجع البيانات.
- **آخر تحديث/تغيير فعلي بالبيانات**: آخر مرة تغيرت فيها نتائج أو جدول المباريات فعلاً.

## الملفات المعدّلة

- `scripts/update-worldcup-2026.mjs`
  - أضيف الحقل `metadata.last_checked_at`.
  - عند نجاح الفحص بدون تغييرات، يتم تحديث `last_checked_at` فقط مع إبقاء `last_updated` كما هو.
  - عند وجود تغيير فعلي بالمباريات/النتائج، يتم تحديث `last_updated` و `last_checked_at` معاً.

- `public/index.html`
- `public/index_phone.html`
  - تم تغيير النص الظاهر من **آخر تحديث** إلى **آخر فحص**.
  - الواجهة تعرض `last_checked_at` إذا كان موجوداً، وترجع إلى `last_updated` للنسخ القديمة.

## النتيجة

بعد رفع هذه النسخة وتشغيل Action كأس العالم، لن يبقى القسم عالقاً على الجمعة 5 حزيران إذا لم تتغير البيانات؛ سيعرض وقت آخر فحص ناجح.
